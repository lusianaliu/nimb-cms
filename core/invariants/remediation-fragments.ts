const WRITABLE_PROJECT_ROOT_FRAGMENT = 'or choose a writable project root.';

export const formatWritableDirectoryRemediation = (scope: string) =>
  `Grant write permissions for ${scope}, ${WRITABLE_PROJECT_ROOT_FRAGMENT}`;

