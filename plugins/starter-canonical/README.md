# starter-canonical

Minimal canonical plugin example for Nimb CMS.

Demonstrates:
- `manifest.json` with `{ name, version, main }`
- `default register(api)` entrypoint
- field type registration
- content type registration
- plugin route registration
- admin nav registration
