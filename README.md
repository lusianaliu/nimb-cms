# nimb-cms

Nimb CMS runtime and CLI.

## Installability & deployment quick path

For the canonical operator flow, read:

- [`docs/install-deployment.md`](docs/install-deployment.md)

Quick commands:

```bash
npx nimb init my-site
cd my-site
npm install
npx nimb guide
npx nimb setup
npx nimb
```
